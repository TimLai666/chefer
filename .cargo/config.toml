# musl 靜態目標（guest-agent）統一用 rust-lld 連結：
# - Windows host 交叉編譯無 musl cc，rust-lld 隨 rustup 內建即可連結純 Rust 靜態檔
# - Linux/macOS host 同樣適用，行為一致
[target.x86_64-unknown-linux-musl]
linker = "rust-lld"

[target.aarch64-unknown-linux-musl]
linker = "rust-lld"
